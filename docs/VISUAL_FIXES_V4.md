# Visual fixes v4

Исправлено:

- экран «Помощь» больше не падает из-за обращения к `uiStyles.cardTitle`; стили вынесены в локальные `contactTitle/contactSubtitle`, а экспорт `uiStyles` перенесён после создания `StyleSheet`;
- баннеры на главной получили плавное перелистывание через `Animated`, поддержку свайпа влево/вправо, tap по боковым карточкам и автоперелистывание;
- убран эффект внешней «обёртки»: основной фон экранов сделан белым, нижнее меню растянуто на всю ширину, уменьшены внешние боковые отступы;
- добавлены индикаторы активного баннера.
